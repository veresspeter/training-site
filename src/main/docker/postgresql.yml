version: '2'
services:
  maxmove-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/maxmove/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=maxmove
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
